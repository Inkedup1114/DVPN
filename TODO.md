Project TODOs and roadmap
=========================

This file was generated by scanning the repository for incomplete implementations, placeholders, and risky patterns (panics, TODO/FIXME, simplified algorithms, or external system assumptions).

How to use
- Read tasks grouped by area (blockchain, mining, vpn, p2p, payments, api, config, tests).
- Each task has: location(s), rationale, priority, estimated effort, acceptance criteria.
- Pick a task, implement, run `go build ./...` and unit tests, then mark complete here or in your issue tracker.

Summary of findings
- Many packages contain "simplified" or stubbed implementations (ProgPoW, PoW validation, mining backends, WireGuard management) and several places call panic or assume system utilities (wgctrl, sudo ip link) without graceful handling.
- No explicit inline TODO/FIXME tokens were found by a global grep, but many functions contain comments like `/*...*/` in the scanned excerpts indicating omitted or unimplemented code regions. The scanner also found places with `panic()` calls and simplified algorithm implementations that need attention.

High-priority tasks
1) Harden WireGuard VPN components
   - Files: `pkg/vpn/wireguard/server.go`, `pkg/vpn/wireguard/client.go`, `pkg/vpn/vpn.go`
   - Rationale: Current code uses `panic()` and `sudo ip link` exec calls; adding graceful error handling, configuration validation, and non-root alternatives improves reliability and testability.
   - Priority: High
   - Effort: 3-6 points
   - Acceptance criteria:
     - Replace panics with returned errors where appropriate.
     - Add config validation in `pkg/config/config.go` for VPN fields.
     - Abstract system calls so they can be mocked in tests (introduce an interface for system network ops).

2) Replace simplified PoW/ProgPoW placeholders with clear contracts or implementations
   - Files: `pkg/blockchain/blockchain.go`, `pkg/mining/progpow/progpow.go`, `pkg/mining/progpow/*`, `pkg/mining/miner.go`
   - Rationale: Current implementation explicitly notes it's simplified. Either implement the full ProgPoW (hard, long) or define clear interfaces and plug-in points so the simplified code is explicit and tested.
   - Priority: High
   - Effort: Large (research + code) if full ProgPoW; Medium if adding interfaces and thorough tests
   - Acceptance criteria:
     - Add an interface for PoW verification and plug current simplified algorithm behind it.
     - Add unit tests that validate current behavior and ensure failures are handled.
     - Document where to add a production ProgPoW implementation.

3) Mining backends: CPU, CUDA, OpenCL
   - Files: `pkg/mining/cpu/cpu_miner.go`, `pkg/mining/cuda/cuda_miner.go`, `pkg/mining/miner.go`
   - Rationale: Many functions are stubs or use CGo calls (`cuda_progpow_mine`) that can fail on systems without CUDA. Improve graceful degradation and configuration flags.
   - Priority: High
   - Effort: Medium
   - Acceptance criteria:
     - Make CUDA backend optional and detect availability at runtime.
     - Document how to build with CGO/CUDA and handle missing shared libs.
     - Add a safe fallback to CPU miner with tests.

Medium-priority tasks
4) P2P robustness and error logging
   - Files: `pkg/p2p/node.go`, `pkg/p2p/protocol/messages.go`
   - Rationale: Handshake contains debug prints and simplistic JSON over net.Conn handling without timeouts or framing. Add timeouts, proper framing, and better error logging.
   - Priority: Medium
   - Effort: Medium
   - Acceptance criteria:
     - Implement read/write deadlines on connections.
     - Use a length-prefixed wire format or reuse existing protocol framing.
     - Add unit/integration tests for invalid/malformed messages.

5) Payments / Bandwidth proofs
   - Files: `pkg/payments/bandwidth_proof.go`, `pkg/payments/channels/channel.go`
   - Rationale: Critical for security: ensure deterministic hashing, signature validation, and concurrency safety.
   - Priority: Medium
   - Effort: Medium
   - Acceptance criteria:
     - Add tests for `calculateProofHash`, `CreateProof`, signature validation, and concurrent session updates.
     - Ensure sessions are cleaned up properly and do not leak memory.

Lower-priority tasks
6) API handlers and servers
   - Files: `internal/api/handlers/*.go`, `internal/api/server.go`
   - Rationale: Handlers contain TODO-like `/*...*/` stubs and simplistic responses. Add proper request validation and error responses.
   - Priority: Low
   - Effort: Small–Medium
   - Acceptance criteria:
     - Implement missing handlers (ConnectPeer, GetPeers, mining endpoints) or return standardized errors.
     - Add unit tests for handlers using httptest.

7) Config improvements and secret handling
   - Files: `pkg/config/config.go`, `configs/*.yaml`
   - Rationale: `GetVPNPrivateKey` reads files and trims; add clearer precedence, support env vars, and avoid storing private keys in repo.
   - Priority: Low
   - Effort: Small
   - Acceptance criteria:
     - Support reading private keys from env var (with clear precedence) and validate key format.
     - Document recommended key management in README.

8) Tests and CI
   - Files: `test/*`, GitHub Actions or Makefile (none found in scan)
   - Rationale: Add CI pipeline to build/test/lint on PRs.
   - Priority: Low
   - Effort: Medium
   - Acceptance criteria:
     - Add a GitHub Actions workflow or Makefile target that runs `go vet`, `go test ./...`, and `golangci-lint` if desired.

Quick wins (low risk, high value)
- Replace panic calls with errors in `pkg/vpn/*` and `pkg/vpn/wireguard/*`.
- Add unit tests for `pkg/blockchain` and `pkg/payments` that cover basic success/failure cases.
- Introduce a `PoWVerifier` interface and wire it into `blockchain` to make future ProgPoW swaps easier.

Notes and assumptions
- The scanner did not find explicit lines labeled `TODO` or `FIXME`, but many functions were elided in the code excerpts and contain `/*...*/` indicating omitted or unimplemented code. Those are treated as gaps.
- Some tasks (CUDA, WireGuard) require elevated privileges or hardware and may not run in CI; for those, implement detection and fallbacks.

Next steps
1. Pick one High-priority task to start (e.g., replace panics in WireGuard code with errors).
2. Implement a small change and add a unit test demonstrating behavior.
3. Iterate through the list and create issues for larger items (full ProgPoW, GPU backend improvements).

If you'd like, I can:
- Open PR-style patches for one or two of the quick wins (e.g., replace panics in `pkg/vpn/wireguard/*`).
- Try `go build ./...` and run unit tests and report failures.

---
Generated on: September 21, 2025

Work completed (automated quick win)
----------------------------------
- Replaced panics in `pkg/vpn/wireguard/server.go` and `pkg/vpn/wireguard/client.go` constructors with error returns. Constructors now return `(*WireGuardServer, error)` and `(*WireGuardClient, error)` respectively.
- Updated `cmd/dvpnd/main.go` to handle constructor errors and avoid program panics on initialization failures.
- Fixed unrelated compile-time issues in `pkg/p2p/node.go` (corrected peer removal and stale peer deletion) discovered when running `go build ./...`.
- Ran `go build ./...` and `go test ./...` — build succeeded and tests ran (no test files in most packages; existing tests passed or were cached).

Notes:
- These changes are low-risk and improve robustness by replacing application panics with returned errors and graceful handling. They also make the WireGuard components easier to mock and test.
- Next recommended steps: add unit tests for the WireGuard wrappers (mock `wgctrl.Client`), add an interface to abstract system network operations (exec calls), and add config validation in `pkg/config/config.go` for VPN settings.
